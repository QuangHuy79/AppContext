// src/main.jsx
import React from "react";
import ReactDOM from "react-dom/client";
import AppRoot from "./core/app/AppRoot";

ReactDOM.createRoot(document.getElementById("root")).render(<AppRoot />);
