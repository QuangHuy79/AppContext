🚀 Tổng quan hệ thống AppContext Modules
Mục tiêu: tạo kiến trúc context chuẩn, tái sử dụng được cho mọi project React, gồm nhiều module nhỏ gọn, plug-and-play.

🧩 Step 1 – NetworkContext
✅ Theo dõi trạng thái online/offline (navigator.onLine + window.addEventListener).

Cung cấp: { isOnline }.

Tự động update khi network thay đổi.
🧠 Giúp hiển thị cảnh báo hoặc disable API khi mất mạng.

🧩 Step 2 – DeviceContext
✅ Theo dõi thông tin thiết bị: screenSize, orientation, isMobile.

Cung cấp: { deviceInfo }.

Tự động re-render khi resize.
🧠 Phục vụ responsive layout, analytics, UI adaptive.

🧩 Step 3 – SettingsContext
✅ Quản lý theme + language (và các user setting cơ bản).

Lưu vào localStorage.
🧠 Giữ giao diện consistent sau reload, chuẩn cho dark/light mode.

🧩 Step 4 – AppContext (core)
✅ Dùng useReducer để gom các action chung (UI/SHOW_TOAST, SYSTEM/RESET,...).

Là root context cho mọi module.

Wrap toàn app bằng <AppProvider>.
🧠 Trung tâm điều phối giữa các context con và ToastProvider.

🧩 Step 5 – UIContext
✅ Quản lý UI state: sidebar open, modal, theme mode, layout size.

Liên kết toastService để hiển thị toast từ reducer action.
🧠 Giúp quản lý trạng thái giao diện xuyên suốt app.

🧩 Step 6 – DataContext
✅ Cung cấp data, loadData(), refreshData().

Có loading, error, và tích hợp toastService khi fetch thành công/thất bại.
🧠 Module trung gian kết nối data layer – phù hợp khi thêm API thật.

🧩 Step 7 – DataSyncContext
✅ Cho phép đồng bộ dữ liệu thủ công hoặc định kỳ.

Có syncNow() → hiển thị toast kết quả.
🧠 Hỗ trợ offline-first hoặc auto-sync.

🧩 Step 8 – AuthContext
✅ Quản lý user, token, login, logout, loading.

Giả lập API login, có toast thông báo trạng thái.
🧠 Là module bảo mật, dễ tích hợp JWT sau này.

🧩 Step 9 – DataService + toastService
✅ toastService: bridge giữa ToastProvider và các context.

✅ Cho phép gọi toastService.show() ở bất kỳ module nào mà không cần prop drilling.
🧠 Đây là “trái tim UI feedback” cho toàn bộ kiến trúc.

🧩 Step 10 – NotificationContext
✅ Quản lý danh sách thông báo (type, title, message, read, timestamp).

✅ Gọi toastService tức thời khi thêm notification.
🧠 Là bước đầu để hiển thị trung tâm thông báo cho toàn hệ thống.