🧭 LỘ TRÌNH MODULE HÓA APPCONTEXT (TỪ BƯỚC 4–9)
🔹 Bước 4 – AuthContext
Mục tiêu:
Quản lý thông tin đăng nhập người dùng (token, user info, trạng thái đăng nhập).

Chức năng chính:

Đăng nhập / Đăng xuất / Kiểm tra đăng nhập.

Lưu token + thông tin user vào localStorage.

Tự động khôi phục session khi reload trang.

Hook: useAuth()
Phụ thuộc: storageService, apiService.

🔹 Bước 5 – UIContext
Mục tiêu:
Quản lý trạng thái giao diện (UI states) toàn app.

Chức năng chính:

Sidebar mở/tắt, modal đang hiển thị, loading overlay, theme override, v.v.

Cho phép mở/đóng modal, toggle sidebar, v.v.

Hook: useUI()
Phụ thuộc: none

🔹 Bước 6 – CartContext
Mục tiêu:
Quản lý giỏ hàng (cho các app e-commerce hoặc hệ thống có khái niệm item/order).

Chức năng chính:

Thêm / xóa / cập nhật sản phẩm.

Tính tổng tiền, tổng số lượng.

Lưu vào localStorage.

Hook: useCart()
Phụ thuộc: storageService

🔹 Bước 7 – AppReducer & InitialState (hoàn thiện lõi chung)
Mục tiêu:
Chuẩn hóa hệ thống reducer và state khởi tạo để AppContext có thể gộp toàn bộ modules.

Chức năng chính:

Gom các reducer từ từng context (nếu cần).

Tạo initialState chuẩn hoá (dùng chung).

Chuẩn bị nền cho các tính năng nâng cao (global dispatch).

🔹 Bước 8 – AppContext.jsx (tổng hợp providers)
Mục tiêu:
Tạo AppContext gốc wrap toàn bộ ứng dụng, gồm các provider module + ToastProvider.

Chức năng chính:

Import tất cả module context.

Gói <AppProvider> bao bọc toàn bộ app.

Quản lý ToastProvider và future global components.

🔹 Bước 9 – Hook useApp()
Mục tiêu:
Tạo hook trung tâm để truy cập tất cả context nhanh chóng.

Chức năng chính:

Trả về object gồm tất cả context con (useAuth, useUI, useSettings, …).

Cho phép truy cập mọi state/context từ một hook duy nhất (useApp()).

✨ Kết quả cuối cùng
Bạn sẽ có:

AppContextProvider → combine all modules
└── useApp() → access everything in one place
Hệ thống này có thể drop-in cho bất kỳ React project nào để quản lý state toàn cục mà không cần Redux hay Recoil.