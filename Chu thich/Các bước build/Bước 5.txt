🎯 Mục tiêu Bước 5 – UIContext
🧩 Chức năng:
Quản lý trạng thái giao diện (UI state) toàn cục, ví dụ:

Modal hiển thị / ẩn

Sidebar mở / đóng

Loading overlay (global loading spinner)

Toast trigger (nếu muốn module hóa thêm)

🧱 Cấu trúc file
src/
├── context/
│   └── modules/
│       └── UIContext.jsx
└── hooks/
    └── useUI.js
🧠 Yêu cầu logic:
Tạo UIContext để chia sẻ các state UI toàn cục.

State cơ bản:

{
  isModalOpen: false,
  isSidebarOpen: false,
  isLoading: false,
}
Cung cấp các hàm:

openModal() / closeModal()

toggleSidebar()

setLoading(true/false)

Tích hợp với AppContext thông qua dispatch, để gửi toast hoặc cập nhật global UI state nếu cần.

Dễ dàng truy cập qua hook useUI().