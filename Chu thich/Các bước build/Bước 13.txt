Bước 13 – APIContext (với mock API bằng json-server) trên nền tảng bạn đã ổn định nhé.
Mục tiêu:

Tạo một APIContext và apiService dùng chung cho toàn bộ app — có thể kết nối mock API (json-server) và hiển thị kết quả qua toastService.

🧩 Cấu trúc file thêm mới
src/
├── context/
│   └── modules/
│       └── APIContext.jsx      ← context mới
├── services/
│       apiService.js           ← wrapper fetch + baseURL
└── test/
        TestAPI.jsx             ← test demo gọi API
db.json                         ← mock data cho json-server